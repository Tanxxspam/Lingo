# --- Web framework & server ---
fastapi==0.115.0
uvicorn[standard]==0.30.6

# --- Data validation & settings ---
pydantic==2.9.2
python-dotenv==1.0.1

# --- Database (sessions, events, interventions, outcomes) ---
sqlalchemy==2.0.35
aiosqlite==0.20.0

# --- Razorpay integration (orders, payment methods, payment links) ---
razorpay==1.4.2

# --- Claude API (message phrasing only) ---
anthropic==0.34.2

# --- HTTP client (used by TestClient / async calls) ---
httpx==0.27.2

# --- Testing (rules engine unit tests) ---
pytest==8.3.3
pytest-asyncio==0.24.0  

simulate